import { ShowStatusPipe } from './show-status.pipe';

describe('ShowStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
